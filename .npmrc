# npm configuration for @kvasar/scoped packages
# Ensures accidental publishes don't happen to wrong registry
@kvasar:registry=https://registry.npmjs.org/

# Default to strict access control
access=public
